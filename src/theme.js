// src/theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#004c64",
    },
    secondary: {
      main: "#e8e8e8",
    },
    success: {
      main: "#047b9d",
    },
  },
});

export default theme;
